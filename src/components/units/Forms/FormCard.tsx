import styled from "styled-components";
import BlueCard from "../Cards/BlueCard";

const FormCard = styled(BlueCard)`
    width: 30em;
    height: 18.2em;
    background: url("/images/bg-input-card.png") center/100% 100% no-repeat;
    align-items: center;
    display: flex;
    justify-content: center;
    flex-direction: column;
`;

export default FormCard;
