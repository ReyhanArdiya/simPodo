describe("Calendar slice selected date field", () => {
	describe("actions", () => {
		it.todo("updates the selected date");
		it.todo("updates the selected month");
		it.todo("updates the selected year");
	});

	describe("selectors", () => {
		it.todo("selects the date");
		it.todo("selects the month");
		it.todo("selects the year");
		it.todo("selects the full date in ISO 8601");
	});
});

describe("Calendar slice viewed date field", () => {
	describe("actions", () => {
		it.todo("updates the viewed date");
		it.todo("updates the viewed month");
		it.todo("updates the viewed year");
	});

	describe("selectors", () => {
		it.todo("selects the date");
		it.todo("selects the month");
		it.todo("selects the year");
		it.todo("selects the full date in ISO 8601");
		it.todo("derives daysInMonth");
		it.todo("derives firstDayOfMonth");
	});
});
